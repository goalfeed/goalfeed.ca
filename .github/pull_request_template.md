## What changed

<!-- Which page(s)/files changed, and what did you change about them? -->

## Why

<!-- What problem does this solve — broken content, a new doc, a design fix? Link an issue if there is one. -->

## How was this tested?

<!-- e.g. previewed with `cd site && python3 -m http.server 8099`, checked in Chrome/Safari/mobile width, etc. -->

## Checklist

- [ ] Previewed locally (`cd site && python3 -m http.server 8099`) and it looks right
- [ ] Used existing design tokens from `site/styles.css` (colors, spacing, type) rather than new one-off values
- [ ] Reused existing header/footer/component markup rather than introducing new ad hoc structure
- [ ] No bundler, framework, or build step introduced — this site stays plain HTML/CSS
- [ ] Links checked (internal links and any links to `github.com/goalfeed/*`)
